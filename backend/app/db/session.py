"""DB session factory -- placeholder, intentionally not wired up this pass.

See app/core/db.py's note -- current DB access is two independent
psycopg_pool.ConnectionPool instances, not a SQLAlchemy session.
"""
