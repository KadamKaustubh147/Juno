"""Shared FastAPI dependencies (DB sessions, current-user, etc.) -- placeholder.

Nothing here yet: there's no auth/user system, and services get their DB access
directly from app/core/db.py's connection_pool rather than via a dependency.
"""
