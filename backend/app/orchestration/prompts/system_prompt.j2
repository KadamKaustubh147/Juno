{# Placeholder -- no prompt templates exist yet.
   Closest current equivalent: the system_parts assembly (memory_context + summary)
   inline in nodes/generate_response.py, as plain f-strings, not a template file. #}
