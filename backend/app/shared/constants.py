"""App-wide constants."""

# Interim values for a therapy_sessions row that's auto-created on a thread's first
# message. No scripts feature exists yet, so these are stand-ins; the scripts/orchestration
# work will set real ones.
DEFAULT_SCRIPT_ID = "cbt_intro_v1"
DEFAULT_SECTION = "intro"
