"""App-wide constants."""

# Stored on every therapy_sessions row. There's no scripts feature to pick between scripts yet,
# so this is the one script the graph runs (orchestration/script.json).
DEFAULT_SCRIPT_ID = "cbt_intro_v1"
