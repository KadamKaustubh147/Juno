# The runtime dependencies the code actually imports. pyproject.toml is the source of
# truth (use `uv sync`); it still also lists mlflow, mlflow-skinny, pandas and flask,
# which nothing imports any more.
fastapi[standard]>=0.141.1
uvicorn
langchain>=1.3.16
langchain-openai>=1.6.0
langgraph>=1.2.11
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary,pool]>=3.2.0
sqlalchemy>=2.0.52
alembic>=1.19.1
pgvector>=0.5.0
fastembed>=0.4.0
python-dotenv>=1.2.3
